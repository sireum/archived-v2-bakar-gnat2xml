with "asis";

project Avatox_NoXSL is

   for Source_Dirs use (".", "mckae", "strings_edit_subset", "no_xslt");
   for Object_Dir use ".";
   for Main use ("avatox.adb");

   package Binder is
   end Binder;

   package Compiler is
      for Default_Switches ("ada") use ("-O2", "-gnatQ", "-gnat05");
   end Compiler;

   package Builder is
      for Executable ("avatox.adb") use "";
   end Builder;

   package Linker is
   end Linker;

end Avatox_NoXSL;

